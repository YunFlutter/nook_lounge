class AppSpacing {
  const AppSpacing._();

  /// 유지보수 포인트:
  /// 기본 간격 규칙(보통 10px)
  static const double s10 = 10;

  /// 유지보수 포인트:
  /// 페이지 기본 좌우 여백(20px)
  static const double pageHorizontal = 20;

  /// 유지보수 포인트:
  /// 도감 화면 전용 좌우 여백(10px)
  static const double catalogHorizontal = 10;

  /// 유지보수 포인트:
  /// 모달 여백 규칙(바깥 10px, 내부 20px)
  static const double modalOuter = 10;
  static const double modalInner = 20;
}
